# TODO - Single-click login

- [ ] Inspect backend + frontend auth implementation (already started: Angular layout + backend main routes).
- [ ] Decide what “single click login” means in this app context (likely auto-select passkey/face or auto-submit login for known user).
- [ ] Implement UI change: add a single button on the login screen that triggers the best available auth method (passkey > face > password).
- [ ] Implement logic in `Layout` component to perform the single-click flow.
- [ ] Add translations/labels if needed.
- [ ] Build/test: run frontend build and verify auth flow works.

